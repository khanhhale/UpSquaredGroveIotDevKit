1) Installation of packages
pip install --upgrade pip
sudo pip install google-cloud oauth2client apiclient google-api-python-client cryptography requests Flask-JWT python-dateutil

sudo apt-get python-tk python-qt4 python-setuptools
sudo pip install pyjwt google-gax
sudo pip install Pillow

2) Install protobuf3-to-dict-0.1.5:
cd camera/protobuf3-to-dict-0.1.5
python ./setup.py install

3) Install PylonCamera:
cd camera/PylonCamera
python ./setup.py install

4) Create private and public keys
openssl req -x509 -newkey rsa:2048 -keyout rsa_private.pem -nodes -out rsa_cert.pem -subj "/CN=unused"

5) Add environment variable for service account json file to linux startup file:
export GOOGLE_APPLICATION_CREDENTIALS="/ath_to_camera_script_folder/PemFiles/cloud_iot_service_account_json_file"

Note:
    1) Replace cloud_iot_service_account_json_file with your service account json file
    2) Replace path_to_camera_script_folder with your path to camera_script folder

6) Install the script as a service that automatically starts or restarts the application when the operating system boots up.
sudo cp /cloud_iot_service_account_json_file/camera_startup /etc/init.d
sudo chmod +x /etc/init.d/camera_startup
sudo update-rc.d camera_startup defaults



sudo python Main.py --project_id=cloud-iot-testing-185623 --registry_id=cloud-iot-registry2 --device_id=device-3 --message_type=event --algorithm=RS256 --private_key_file=/home/upsquared/ups_grove/PemFiles/rsa_private.pem --public_key_file=/home/upsquared/ups_grove/PemFiles/rsa_cert.pem --credential=/home/upsquared/ups_grove/PemFiles/cloud-iot-testing-052b9ca41b45.json --cloud_region=us-central1 --message_type=event --message_data_type=data_string --message="sending test data 3 for upsquared iot"
sudo add-apt-repository ppa:mraa/mraa
sudo apt-get update
sudo apt-get install libmraa1 libmraa-dev libmraa-java python-mraa python3-mraa node-mraa mraa-tools



